ngapp.service('vortexService', function() {
    this.getModManagerPath = function(settings) {

    };

    this.getModsPath = function(settings) {

    };

    this.detect = function(settings) {

    };
});
